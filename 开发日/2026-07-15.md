# 开发日 · 2026-07-15

## 阶段

打包 + 开机自启（D 盘）已完成

## 已完成

- [x] MVP 功能 D0–D6（文字/图片、再复制、置顶删除搜索、浅粉 UI）
- [x] 工程复制到 `D:\vibe-coding\history_cv_tool`
- [x] D: 上 `npm install`；`next build` 静态导出成功（无 EISDIR）
- [x] electron-builder 产出 `dist\历史粘贴板 Setup 0.1.0.exe`（约 120MB）
- [x] 开机自启 IPC + UI 开关；`signAndEditExecutable: false` 规避符号链接权限问题
- [x] 新增 `docs/06-release.md`

## 待办

- [ ] 用户安装并确认：不插 E 盘可运行；勾选开机自启后重启生效
- [ ] （可选）自定义应用图标；开启 Windows 开发人员模式下的符号链接（若以后要改可执行文件元数据）

## 风险与阻塞

- SmartScreen 可能拦截未签名安装包
- C: 空间紧：缓存勿落在 C:，已用 `D:\vibe-coding\*` 缓存目录

## 明日建议

按 `docs/06-release.md` 安装体验；确认无问题后可删 `node_modules` 省磁盘。
